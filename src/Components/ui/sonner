import React from 'react';

export default function Sonner({ title, description }) {
  return (
    <div className="fixed bottom-4 right-4 z-[100] w-full max-w-sm bg-black border border-white/10 p-4 rounded-xl shadow-2xl animate-in slide-in-from-bottom-5">
      <p className="text-sm font-bold text-white">{title}</p>
      {description && <p className="text-xs text-gray-500 mt-1">{description}</p>}
    </div>
  );
}