export class ContactGroup {
  constructor({ id, name, description, color }) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.color = color; // Pour l'affichage UI
  }
}