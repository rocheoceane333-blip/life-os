export class Secret {
  constructor({ id, label, encryptedContent, category, createdAt = new Date() }) {
    this.id = id;
    this.label = label;
    this.encryptedContent = encryptedContent;
    this.category = category; // ex: 'Banking', 'Private Note'
    this.createdAt = createdAt;
  }
}