export class UserSession {
  constructor({ id, userId, deviceName, ipAddress, lastActive = new Date(), isActive = true }) {
    this.id = id;
    this.userId = userId;
    this.deviceName = deviceName;
    this.ipAddress = ipAddress;
    this.lastActive = lastActive;
    this.isActive = isActive;
  }
}